<%= form_with(model: capital, local: true) do |form| %>
  <% if capital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(capital.errors.count, "error") %> prohibited this capital from being saved:</h2>

      <ul>
        <% capital.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-4 my_form">
      <%= form_for(@capital) do |form| %>
        <%= form.label "Назва" %>
        <%= form.text_field :name, class: "form-control" %>

        <%= form.label "Населення" %>
        <%= form.number_field :population, class: "form-control" %>

        <%= form.label "Країна" %>
        <%= form.collection_select(:country_id, @countries, :id, :name, {}, { :class => "form-control" }) %>

        <%= form.submit "ОК", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
<% end %>
